<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link rel="stylesheet" href="/css/bootstrap.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f2f2f2;
        }

        .container {
            display: flex;
            width: 800px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .left-side {
            background-image: url("https://wallpapers.com/images/high/camilo-villegas-on-downhill-road-xhr9o6nqecpi0xle.webp");
            background-size: cover;
            background-position: center;
            width: 50%;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .left-side .content {
            text-align: center;
        }

        .left-side h2 {
            font-size: 24px;
            margin-bottom: 10px;
        }

        .left-side p {
            font-size: 16px;
            margin-bottom: 20px;
        }

        .left-side button {
            background-color: #ff6a00;
            border: none;
            color: white;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
            border-radius: 5px;
        }

        .right-side {
            width: 50%;
            background-color: white;
            padding: 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .right-side h2 {
            font-size: 24px;
            color: #333;
            margin-bottom: 20px;
            text-align: center;
        }

        .floating-label {
            position: relative;
            margin-bottom: 20px;
        }

        .floating-label input[type="text"],
        .floating-label input[type="password"] {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: none;
            border-bottom: 1px solid #ccc;
            outline: none;
            font-size: 16px;
            transition: border-color 0.3s;
        }

        .floating-label input[type="text"]:focus,
        .floating-label input[type="password"]:focus {
            border-bottom: 1px solid #ff6a00;
        }

        .floating-label label {
            position: absolute;
            left: 0;
            top: 15px;
            font-size: 16px;
            color: #666;
            transition: all 0.3s;
            pointer-events: none;
        }

        .floating-label input[type="text"]:focus+label,
        .floating-label input[type="text"]:not(:placeholder-shown)+label,
        .floating-label input[type="password"]:focus+label,
        .floating-label input[type="password"]:not(:placeholder-shown)+label {
            top: -10px;
            font-size: 12px;
            color: #ff6a00;
        }

        .checkbox {
            display: flex;
            align-items: center;
            margin-bottom: 20px;
        }

        .checkbox input[type="checkbox"] {
            margin-right: 5px;
        }

        .right-side button {
            width: 100%;
            background-image: linear-gradient(to right, #ff6a00, #ff9900);
            border: none;
            color: white;
            padding: 10px;
            font-size: 16px;
            cursor: pointer;
            border-radius: 5px;
            margin-bottom: 15px;
        }

        .right-side .signup-button {
            width: 100%;
            background-color: #333;
            color: white;
            padding: 10px;
            font-size: 16px;
            cursor: pointer;
            border-radius: 5px;
            border: none;
            text-align: center;
            text-decoration: none;
            display: inline-block;
        }

        .right-side a {
            display: block;
            text-align: center;
            color: #666;
            text-decoration: none;
            margin-top: 15px;
            font-size: 14px;
        }

        .right-side a:hover {
            text-decoration: underline;
        }
        .google-signup {
            width: 100%;
            background-color: white;
            border: 1px solid #ccc;
            color: #333;
            padding: 10px;
            font-size: 16px;
            cursor: pointer;
            border-radius: 5px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-top: 10px;
        }

        .google-signup img {
            width: 20px;
            margin-right: 10px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="left-side">
            <div class="content">
                <h1><strong>CYCLORE</strong></h1>
                <h2>New to our website?</h2>
                <p>"Pedal forward with purpose – your journey begins here."</p>
            </div>
        </div>
        <div class="right-side">
            <h2>Log in to Enter</h2>
            <form action="/login" method="post">
                <input type="text" id="message" value="{{{message}}}" name="message" hidden>
                <div class="floating-label">
                    <input type="text" id="email" name="email" placeholder=" " required>
                    <label for="email">Email</label>
                </div>
                <div class="floating-label">
                    <input type="password" id="password" name="password" placeholder=" " required>
                    <label for="password">Password</label>
                    {{#if message}}
                    <div class="alert alert-danger">{{message}}</div>
                    {{/if}}
                </div>
                <button type="submit">Log In</button>
                <a href="/forgot-password">Forgot Password?</a>
            </form>
            <a href="/auth/google">
            <div class="google-signup">
                <img src="https://i.pinimg.com/236x/39/21/6d/39216d73519bca962bd4a01f3e8f4a4b.jpg" alt="Google Logo">
                Sign Up with Google
            </div>
            </a>
            <a href="/register" class="signup-button">Sign Up</a>
        </div>
    </div>
</body>

</html>